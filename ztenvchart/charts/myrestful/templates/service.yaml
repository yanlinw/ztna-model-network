apiVersion: v1
kind: Service
metadata:
  name: {{ include "myrestful.fullname" . }}
  labels:
    {{- include "myrestful.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myrestful.selectorLabels" . | nindent 4 }}
